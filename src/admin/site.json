{
  "name": "admin",
  "type": "built-in",
  "subdomain": "admin",
  "main": "index.ts",
  "description": "Built-in admin panel for Dial Up Deploy",
  "scripts": {
    "dev": "bun index.ts",
    "start": "bun index.ts"
  },
  "config": {
    "enableByDefault": true,
    "requiredDuringSetup": true,
    "port": null,
    "authentication": "required"
  }
}