{
  "name": "@dialup-deploy/server",
  "version": "0.1.0",
  "description": "Server for DialUpDeploy",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --minify"
  },
  "dependencies": {
    "@dialup-deploy/core": "workspace:*",
    "croner": "^9.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
