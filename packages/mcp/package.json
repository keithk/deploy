{
  "name": "@keithk/deploy-mcp",
  "version": "0.1.0",
  "description": "MCP server for Dial Up Deploy",
  "main": "dist/index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --minify"
  },
  "dependencies": {
    "@keithk/deploy-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keithk/deploy.git",
    "directory": "packages/mcp"
  }
}
