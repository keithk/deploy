{
  # Use project directory for storage
  storage file_system {
    root /Users/keith/projects/deploy/config/caddy-data
  }

  # Use local certificates
  local_certs
  
  # Log to console for debugging
  debug
}

https://*.dev.deploy, https://dev.deploy {
  reverse_proxy localhost:3000
}
