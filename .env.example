# Project domain for local development
PROJECT_DOMAIN=dev.flexi
PORT=3000
ROOT_DIR=./sites

# Encryption key for site secrets (env_vars) at rest.
# Generate one with: openssl rand -base64 32
# If unset, secrets are stored in plaintext (not recommended for production).
DEPLOY_ENCRYPTION_KEY=
